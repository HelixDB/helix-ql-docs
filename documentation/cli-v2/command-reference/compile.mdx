---
title: "helix compile"
sidebarTitle: "compile"
---

> For the complete documentation index optimized for AI agents, see [llms.txt](/llms.txt).

Compile project queries into executable format.

## Usage

```bash
helix compile [OPTIONS]
```

## Available flags

| Flag | Type | Description |
|------|------|-------------|
| `--path`, `-p` | String | The path to the project |
| `--output`, `-o` | String | The output path for compiled queries |

## Examples

```bash
# Compile queries in the current project
helix compile

# Compile with a specific output path
helix compile --output ./build
```
