---
title: "Query Structure"
description: "Display inline code and code blocks"
icon: "code"
---

### Basic Query Syntax

```rust
QUERY QueryName(param1, param2) =>
    result <- traversal_expression
    RETURN result
```

### Components:

- `QUERY`: Keyword used to start a query definition
- `QueryName`: User provided Identifier for the query
- `parameters`: Input parameters in parentheses
- `=>`: Separates query header from body
- `<-`: Assignment operator
- `RETURN`: Specifies output values
