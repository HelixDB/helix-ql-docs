---
title: 'Installation'
description: 'Get started with HelixDB installation on different platforms'
---

# Installing HelixDB

HelixDB can be used locally by first installing the HelixCLI.

## HelixCLI installation
First, run the following command to install the HelixCLI:

```bash
curl -sSL https://install.helix-db.com | bash
```



## Verifying Installation

Next, verify that the HelixCLI is installed correctly by running the following command:

```bash
helix --version
```





## Next Steps

<CardGroup cols={2}>
  <Card title="Configuration (Coming Soon)" icon="gear" href="essentials/configuration">
    Learn how to configure HelixDB for your needs
  </Card>
  <Card title="Schema Definition" icon="code" href="../../essentials/schema-definition">
    Define your data model with HQL schemas
  </Card>
  <Card title="Client Libraries (Coming Soon)" icon="plug" href="essentials/client-libraries">
    Connect to HelixDB from your application
  </Card>
</CardGroup>